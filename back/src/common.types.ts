export enum TriggerType {
  NONE = 'NONE',
  CROSSING_UP = 'CROSSING_UP',
  CROSSING_DOWN = 'CROSSING_DOWN',
}

export enum StatusType {
  ACTIVE = 'ACTIVE',
  PENDING = 'PENDING',
  PAUSED = 'PAUSED',
  DONE = 'DONE',
}
